# AWS SDK for Python
boto3==1.40.44

# PostgreSQL driver (binary version - works well on Lambda)
psycopg2-binary==2.9.10

# HTTP requests module
requests==2.32.5

# JSON schema validation (optional but useful for Lambda inputs)
jsonschema==4.25.1

# AWS Lambda Powertools for Python (logging, tracing, metrics)
aws-lambda-powertools==3.20.0

