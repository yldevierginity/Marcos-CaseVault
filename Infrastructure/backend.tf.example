# Terraform Backend Configuration
# 
# For local development, comment out this block or don't create backend.tf
# For CI/CD, create backend.tf with this content:

# terraform {
#   backend "s3" {
#     bucket         = "your-terraform-state-bucket"
#     key            = "casevault/terraform.tfstate"
#     region         = "ap-southeast-1"
#     encrypt        = true
#     dynamodb_table = "terraform-state-lock"
#   }
# }

# To create the S3 backend:
# 1. Create S3 bucket for state:
#    aws s3 mb s3://your-terraform-state-bucket --region ap-southeast-1
#
# 2. Enable versioning:
#    aws s3api put-bucket-versioning \
#      --bucket your-terraform-state-bucket \
#      --versioning-configuration Status=Enabled
#
# 3. Create DynamoDB table for locking:
#    aws dynamodb create-table \
#      --table-name terraform-state-lock \
#      --attribute-definitions AttributeName=LockID,AttributeType=S \
#      --key-schema AttributeName=LockID,KeyType=HASH \
#      --billing-mode PAY_PER_REQUEST \
#      --region ap-southeast-1
#
# 4. Copy this file to backend.tf and uncomment
# 5. Run: terraform init -migrate-state
